<%= form_with(model: service) do |form| %>
  <% if service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
        <% service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :vehicle_id %>
    <%= form.collection_select(:vehicle_id, Vehicle.all, :id, :patent) %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <!-- <div class="field">
    <%#= form.label :kilometres %>
    <select name="service[kilometres]">
      <option value="<%#= service.distance %>"><%#= service.distance %></option>
    </select>
  </div> -->

  <div class="field">
    <%= form.label :begins_at %>
    <%= form.date_select :begins_at %>
  </div>

  <div class="field">
    <%= form.label :ends_at %>
    <%= form.date_select :ends_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
